# 打字练习 + 键盘全键测试网页 V1 · 测试用例文档

> 角色：测试工程师 Taylor · 用例版本：V1.2（自适应换行对齐版：在 V1.1 裁决闭环版基础上对齐 PM 裁决7「固定字数切行 → 自适应换行」；附录新增 Q7 裁决记录（验证状态：待验证，待任务 #13 E2E 闭环）；TC-ROW 系列与响应式用例预期修订为自适应口径，新增 TC-ROW-013/014/015）
> 编写依据：`design-doc.md`（V1 decision-complete，含裁决7 自适应换行算法）§5 信息架构、§6 交互规范（§6.8 自适应行切分）、§7 数据契约、§8 视觉规范、§9 关键技术正确性、§10 边界与异常（B1–B19）、§11 验收标准（A1–A17）、§12 原型图（P1–P5）
> 追溯原则：每条用例至少映射一个验收项（A/B 编号）；A1–A17 与 B1–B19 每项至少被一条用例覆盖（见 §2 覆盖矩阵）

---

## 1. 用例统计

### 1.1 总量与分布

| 维度 | 分布 |
| --- | --- |
| 用例总数 | **78** |
| 按优先级 | P0：41 · P1：28 · P2：9 |
| 按类型 | 功能：39 · UI：11 · 边界：17 · 回归：8 · 兼容：3 |
| 按模块 | 兼容运行 5 · 打字核心 15 · 逐行配对 15 · IME 7 · 键盘测试 13 · 主题 8 · 视图切换 4 · 边界异常 7 · 视觉规范 4 |

### 1.2 模块清单

| 模块前缀 | 模块 | 用例数 | 主要验收项 |
| --- | --- | :--: | --- |
| TC-COMPAT | 兼容与运行 | 5 | A1, A2, A3, A15, B16 |
| TC-TYPE | 打字练习核心 | 15 | A4, A5, A6, A12, B15 |
| TC-ROW | 逐行配对输入（核心） | 15 | A17, A5, A13, B1, B5, B15 |
| TC-IME | 中文 IME | 7 | A7, B8, B14 |
| TC-KB | 键盘全键测试 | 13 | A8, A9, A14, B2, B12, B13 |
| TC-THEME | 主题切换与持久化 | 8 | A10, A11, B6, B7, B9 |
| TC-TAB | 视图切换 | 4 | A9, B17 |
| TC-EDGE | 边界与异常 | 7 | B1–B5, B10, B11, A13, A14 |
| TC-VIS | 视觉规范 | 4 | A16, A10 |

### 1.3 测试环境与通用约定

- 浏览器：Chrome / Edge 近期版本（主），Firefox / Safari（抽测）；OS：Windows。
- 打开方式：`file://` 双击 与 本地 HTTP 服务器（`python -m http.server` 等）两种均须执行兼容模块用例。
- 输入法：微软拼音（主）、搜狗拼音（抽测）；英文态用系统默认。
- 观测工具：DevTools Console（报错计数）、Network（CDN/脚本顺序）、Elements（`data-theme`、class、CSS 变量计算值）、Application→Local Storage（`tp_theme`）、Console 直接读取 `window.TP_ARTICLES` / `window.TP_KEYBOARD_LAYOUT` 做数据契约核对。
- 通用前置（除特别注明）：联网、页面已正常加载、当前为打字视图。
- 判定口径：Console「0 error」指无 error 级日志（warning 不计）；色值核对以 DevTools 计算样式为准，允许渲染舍入误差。

---

## 2. 验收项覆盖矩阵

### 2.1 验收标准 A1–A17 → 用例

| 验收项 | 覆盖用例 |
| --- | --- |
| A1 可直接运行 | TC-COMPAT-001, TC-COMPAT-002 |
| A2 无构建/脚本顺序 | TC-COMPAT-003 |
| A3 Vue 构建正确 | TC-COMPAT-004 |
| A4 打字核心 | TC-TYPE-001~005, TC-ROW-002 |
| A5 统计核心 | TC-TYPE-006~009, TC-ROW-012 |
| A6 完成弹窗 | TC-TYPE-011~013, TC-ROW-010, TC-EDGE-006 |
| A7 中文 IME | TC-TYPE-010, TC-IME-001~007 |
| A8 键盘测试 | TC-KB-001~008, TC-KB-011~013 |
| A9 视图切换 | TC-TAB-001~004, TC-KB-009, TC-KB-010 |
| A10 主题切换 | TC-THEME-001, 002, 003, 008, TC-VIS-002 |
| A11 主题持久化 | TC-THEME-004, TC-THEME-005 |
| A12 20 篇可切换 | TC-TYPE-001, 004, 013, 014, 015 |
| A13 响应式 | TC-EDGE-005, TC-ROW-011, TC-ROW-014 |
| A14 健壮性（B 全项） | TC-EDGE-001~007, TC-ROW-015 及全部映射 B 项的用例（IME/KB/THEME/COMPAT 模块边界用例） |
| A15 CDN 降级 | TC-COMPAT-005 |
| A16 视觉规范 | TC-VIS-001~004, TC-THEME-003 |
| A17 逐行配对输入 | TC-ROW-001~015, TC-TYPE-010, TC-EDGE-001 |

### 2.2 边界与异常 B1–B19 → 用例

| 边界项 | 覆盖用例 |
| --- | --- |
| B1 超长文章多行切分+滚动 | TC-EDGE-001, TC-ROW-007 |
| B2 快速连击/重复按键 | TC-EDGE-002, TC-KB-011 |
| B3 退格至起点 | TC-EDGE-003 |
| B4 特殊字符/标点 | TC-EDGE-004 |
| B5 窗口缩放/窄屏 | TC-EDGE-005, TC-ROW-011, TC-ROW-014 |
| B6 localStorage 禁用 | TC-THEME-006 |
| B7 localStorage 超额 | TC-THEME-007 |
| B8 IME 候选中途取消 | TC-IME-003, TC-IME-004, TC-IME-007 |
| B9 刷新页面主题保留 | TC-THEME-005 |
| B10 完成后继续输入 | TC-EDGE-006 |
| B11 粘贴文本 | TC-EDGE-007 |
| B12 键盘视图未激活时按键 | TC-KB-009, TC-KB-010 |
| B13 不可捕获键 | TC-KB-012 |
| B14 中英输入法切换 | TC-IME-005 |
| B15 除零 | TC-TYPE-006, TC-TYPE-008, TC-ROW-015 |
| B16 CDN 加载失败 | TC-COMPAT-005 |
| B17 定时器泄漏 | TC-TAB-004, TC-TYPE-011 |
| B18 Tab/空格/方向键默认行为 | TC-KB-013 |
| B19 IME 只发 compositionstart 未发 compositionend | TC-IME-007 |

---

## 3. 测试用例正文

### 3.1 模块：兼容与运行（TC-COMPAT）

#### TC-COMPAT-001 file:// 双击打开 index.html 即运行
- **优先级/类型**：P0 · 兼容　**对应验收项**：A1
- **前置条件**：联网；Chrome/Edge；不启动任何本地服务器。
- **测试步骤**：1) 资源管理器双击 `index.html` 以 `file://` 打开；2) 等待加载，检查应用栏、打字视图行列表、统计 HUD 渲染；3) 打开 DevTools Console，进行输入、Tab 切换、主题切换操作 1 分钟后复查 Console。
- **预期结果**：页面即开即用，Vue CDN 加载成功、视图完整渲染；Console 0 error；打字与键盘视图均可用。

#### TC-COMPAT-002 本地 HTTP 服务器打开可运行
- **优先级/类型**：P1 · 兼容　**对应验收项**：A1
- **前置条件**：联网；工作区根目录启动静态服务器（如 `python -m http.server`）。
- **测试步骤**：1) 浏览器访问 `http://localhost:<port>/index.html`；2) 检查两视图渲染与交互；3) 检查 Console 与 Network（各 JS/CSS 均 200）。
- **预期结果**：http 方式下功能与 `file://` 一致；无 404/跨域报错；Console 0 error。

#### TC-COMPAT-003 经典脚本加载、无 type=module、顺序正确
- **优先级/类型**：P0 · 回归　**对应验收项**：A2
- **前置条件**：任一方式打开页面。
- **测试步骤**：1) 审查 `index.html` 源码：所有 `<script>` 均无 `type="module"`；2) 核对引入顺序为 Vue CDN → articles.js → keyboard.js → TypingPractice.js → KeyboardTest.js → app.js；3) Network 面板确认实际执行顺序一致；4) Console 检查无 ES 模块/CORS 相关报错（`file://` 下尤甚）。
- **预期结果**：无 `type="module"`；顺序符合 §4.3；`file://` 下无 “CORS policy / module script” 类报错；无 npm/打包产物引用。

#### TC-COMPAT-004 Vue 全局构建（含模板编译器）渲染 template 字符串
- **优先级/类型**：P1 · 回归　**对应验收项**：A3
- **前置条件**：联网打开页面。
- **测试步骤**：1) Network 面板确认 CDN 文件为 `vue.global.prod.js`（非 runtime-only）且版本锁定 **3.5.42**（主源 unpkg）；2) Console 执行 `Vue.version === '3.5.42'` 与 `!!window.Vue.compile` 验证版本与编译器；3) 检查组件（行列表、键网格、弹窗）均正常渲染；4) 检查 Console 无 “template compiler” 类告警/报错。
- **预期结果**：加载的是含编译器全局构建且版本与 3.5.42 锁定一致；`Vue.compile` 可用；所有 `template` 字符串组件渲染正确、无编译报错。

#### TC-COMPAT-005 Vue CDN 加载失败友好降级
- **优先级/类型**：P1 · 兼容　**对应验收项**：A15, B16
- **前置条件**：DevTools Network 将 CDN 域名（unpkg/jsdelivr）设为 Block/Offline 后打开页面。
- **测试步骤**：1) 仅阻断主源 unpkg 后刷新：检查自动回退备份源 jsdelivr 加载成功、应用正常运行；2) 同时阻断 unpkg 与 jsdelivr 后刷新：检查 `#app` 显示 boot-error 友好降级提示；3) 检查 Console 信息可诊断（非静默白屏）。
- **预期结果**：单源失败由备份源兜底、功能不受影响；双源均失败时不白屏，`#app` 显示 boot-error 友好提示（如「Vue 加载失败，请检查网络连接后刷新」）；无未捕获异常导致页面崩溃。

### 3.2 模块：打字练习核心（TC-TYPE）

#### TC-TYPE-001 文章随机加载与 meta 信息正确
- **优先级/类型**：P0 · 功能　**对应验收项**：A4, A12
- **前置条件**：默认打字视图。
- **测试步骤**：1) 打开页面记录 meta 行「第 N 篇 · X 字」；2) Console 核对 `TP_ARTICLES[N-1].text.length === X`；3) 刷新 10 次记录篇号序列。
- **预期结果**：meta 篇号/字数与数据一致；10 次加载篇号呈随机分布（非固定同一篇）；行列表与统计 HUD 正常渲染。

#### TC-TYPE-002 逐字四态标色正确性
- **优先级/类型**：P0 · 功能　**对应验收项**：A4
- **前置条件**：已加载文章，聚焦隐藏 input（点击行列表）。
- **测试步骤**：1) 不输入：检查首字为橙色光标态、其余为未输入灰；2) 正确输入 3 字：检查已输字绿（#30d158）、第 4 字橙光标、其余灰；3) 故意输错 1 字：检查该字红（#ff453a）；4) DevTools 核对 class 为 `char correct/wrong/cursor` 及计算色值。
- **预期结果**：四态（correct/wrong/cursor/pending）判定与 §6.2 表完全一致；光标态始终位于 `pos` 处且仅一个字；色值符合 §8（暗亮同值）。

#### TC-TYPE-003 退格删除与标色回退
- **优先级/类型**：P0 · 功能　**对应验收项**：A4
- **前置条件**：已输入若干字符（含 1 个错字）。
- **测试步骤**：1) 按 Backspace 一次：检查 `pos` 回退 1、末位输入字符被移除、光标态回退；2) 连续退格至错字位置再重输正确字：检查标色由红变绿；3) 观察输入行同步缩短。
- **预期结果**：退格逐字回退、不越界；标色/输入行/进度实时重算；重输后状态正确。

#### TC-TYPE-004 换一篇随机换篇且不与当前同 id
- **优先级/类型**：P0 · 功能　**对应验收项**：A4, A12
- **前置条件**：记录当前篇号 N。
- **测试步骤**：1) 点击 meta 行「换一篇」10 次，记录每次篇号；2) 检查每次换篇后输入状态重置（pos=0、输入行空、统计归初始值）。
- **预期结果**：每次新篇号 ≠ 换前篇号；10 次内出现多篇不同文章；换篇后进度/统计/标色全部重置为 idle 态。

#### TC-TYPE-005 重新开始重置当前文章
- **优先级/类型**：P0 · 功能　**对应验收项**：A4
- **前置条件**：当前篇已输入约 20 字（含错字）。
- **测试步骤**：1) 点击「重新开始」；2) 检查篇号不变、pos=0、输入行清空、四态复位、统计回初始值（00:00 / 0 字/分钟 / 100%）、进度条 0%；3) 重新输入验证计时从新首字符起算。
- **预期结果**：同篇重开、全部状态清零；计时起点重新锚定首字符。

#### TC-TYPE-006 计时起点=首字符与统计初始值（除零）
- **优先级/类型**：P0 · 功能　**对应验收项**：A5, B15
- **前置条件**：新加载文章，idle 态。
- **测试步骤**：1) 检查 idle 态统计为「用时 00:00 / 速度 0 字/分钟 / 准确率 100%」；2) 点击行列表聚焦但等待 10 秒不输入：检查用时仍 00:00；3) 输入首字符后开始计时：等待 5 秒检查用时约 00:05；4) 全程检查无 NaN/Infinity 出现。
- **预期结果**：计时锚定首字符而非页面加载/聚焦；idle 态不计时；初始值符合 §6.7；除零守卫生效（显示 0 或 100% 等安全值，绝无 NaN/Infinity）。

#### TC-TYPE-007 用时/速度实时刷新与公式正确
- **优先级/类型**：P1 · 功能　**对应验收项**：A5
- **前置条件**：开始输入并持续练习。
- **测试步骤**：1) 输入约 60 个正确字符，用秒表对照用时显示（误差 ≤1s）；2) 手算速度 = 正确字符数 / (elapsed/60000)，与 HUD「速度」对照（取整误差 ≤1）；3) 停止输入 5 秒：检查用时继续增长、速度随时间下降而刷新。
- **预期结果**：用时秒级实时刷新；速度按 §6.5 公式（正确字符/分钟）计算且实时变化；单位「字/分钟」。

#### TC-TYPE-008 准确率实时计算与未输入锁定 100%
- **优先级/类型**：P0 · 功能　**对应验收项**：A5, B15
- **前置条件**：新文章。
- **测试步骤**：1) idle 态确认准确率 100%；2) 输入 9 正确 + 1 错误：检查准确率 90%；3) 退格删除错字并重输正确：检查准确率回升（按当前已输入口径重算）；4) 检查不出现 NaN。
- **预期结果**：准确率 = 正确字符/已输入字符×100 实时刷新；已输入为 0 时锁定 100%；除零守卫生效，绝无 NaN/Infinity。

#### TC-TYPE-009 总进度条随输入推进
- **优先级/类型**：P1 · UI　**对应验收项**：A5
- **前置条件**：新文章，记录总字数 L。
- **测试步骤**：1) idle 态检查进度条宽度 0%（仅底槽）；2) 输入 L/2 字检查填充约 50%；3) DevTools 核对 `.progress-bar` 的 width 样式 = pos/L×100%；4) 退格若干字检查进度回退。
- **预期结果**：进度条宽度与全篇 pos 口径一致、实时联动、可回退；颜色为 `--accent`。

#### TC-TYPE-010 错字不回插原文（错字分离）
- **优先级/类型**：P0 · 功能　**对应验收项**：A7, A17
- **前置条件**：新文章，记录原文第 5 字。
- **测试步骤**：1) 在第 5 字位置故意输错字；2) 检查原文行第 5 字仍为原文字符（仅标红），未被替换为错字；3) 检查错字出现在该行下方输入行对应列；4) Console 核对原文数据长度恒定（`TP_ARTICLES[当前].text.length` 不变）；5) 继续输满该行检查后续字符不错位。
- **预期结果**：原文只读、长度恒定、零错位；错字仅体现在标色与配对输入行；后续对比不受污染。

#### TC-TYPE-011 完成弹窗触发与内容正确
- **优先级/类型**：P0 · 功能　**对应验收项**：A6, B17
- **前置条件**：选择短文或持续输入至末字。
- **测试步骤**：1) 输满全文最后一字；2) 检查弹窗出现：成功徽章 + 「练习完成」+ 用时/速度/准确率三项；3) 核对弹窗数值与完成瞬间 HUD 数值一致（速度为主数值大号强调色）；4) 观察弹窗期间用时不再增长（计时已停）。
- **预期结果**：`pos === text.length` 即弹窗；背景压暗/模糊；三项数据正确且冻结（clearInterval 生效）；按钮为「换一篇」(主)/「再试一次」(次)。

#### TC-TYPE-012 完成弹窗「再试一次」重开当前篇
- **优先级/类型**：P0 · 功能　**对应验收项**：A6
- **前置条件**：完成弹窗已显示，记录当前篇号 N。
- **测试步骤**：1) 点击「再试一次」；2) 检查弹窗关闭、篇号仍为 N、pos=0、输入行清空、统计与进度复位；3) 输入验证可正常重新练习。
- **预期结果**：重开当前篇而非换篇；全部状态复位；可立即重新输入。

#### TC-TYPE-013 完成弹窗「换一篇」随机换篇
- **优先级/类型**：P0 · 功能　**对应验收项**：A6, A12
- **前置条件**：完成弹窗已显示，记录篇号 N。
- **测试步骤**：1) 点击主按钮「换一篇」；2) 检查弹窗关闭、meta 篇号 ≠ N、状态全部复位；3) 重复完成→换篇 5 次记录篇号序列。
- **预期结果**：每次换篇 id 与前篇不同；状态复位；新文章行列表正确切分渲染。

#### TC-TYPE-014 文章库数据契约（20 篇、id 唯一、非空、150–300 字）
- **优先级/类型**：P1 · 回归　**对应验收项**：A12, A14
- **前置条件**：页面已加载。
- **测试步骤**：1) Console 执行检查：`TP_ARTICLES.length === 20`；2) 检查 id 为 1–20 唯一不重复；3) 检查每篇 `text` 为非空字符串且长度 ∈ [150,300]；4) 检查无空串/纯空白文章；5) 结合 TC-TYPE-006/008 验证除零守卫：进度/速度/准确率在任何取值下均显示 0 或 100% 等安全值，绝无 NaN（Q5 裁决口径）。
- **预期结果**：20 篇、id 唯一、长度合规、无空文章；数据满足 §7.1 契约；除零守卫覆盖空值边界（Q5 已闭环）。

#### TC-TYPE-015 换一篇多次切换覆盖多篇（可切换性抽样）
- **优先级/类型**：P2 · 功能　**对应验收项**：A12
- **前置条件**：默认打字视图。
- **测试步骤**：1) 连续点击「换一篇」30 次记录篇号集合；2) 统计覆盖的不同篇数。
- **预期结果**：30 次切换覆盖 ≥8 篇不同文章（随机性抽样判定）；切换过程无渲染错乱、无 Console 报错。

### 3.3 模块：逐行配对输入（TC-ROW，核心）

#### TC-ROW-001 文章按容器宽度自适应切分为 row 且行数正确
- **优先级/类型**：P0 · 功能　**对应验收项**：A17
- **前置条件**：桌面宽屏（≥1024px）；记录当前文章字数 L 与 `.row-source` 内容区宽度。
- **测试步骤**：1) 数行列表 `.row` 容器数量 R；2) 核对切分来源：`lineStarts` 由 `.row-mirror` 逐字符 Range 测量浏览器自然换行得出（不再固定字数/行），R 与 lineStarts 派生行数一致；3) 检查每行原文字数：除末行外各行文本宽度对容器宽度填充率 ≥90%；4) 检查各行原文拼接后与全文完全一致（无丢字/重字，行边界与 lineStarts 全局索引一致）。
- **预期结果**：行字符数由容器实际宽度决定（自适应换行）；除末行外行文本填充率 ≥90%；resize 后行重切分且已输入内容不丢；全文字符完备无遗漏。

#### TC-ROW-002 每行原文四态标色（含跨行边界）
- **优先级/类型**：P0 · 功能　**对应验收项**：A17, A4
- **前置条件**：输入至跨越第 0 行末字、进入第 1 行中部。
- **测试步骤**：1) 检查第 0 行（已输满）全部字为绿/红（按对错）、无光标态；2) 检查第 1 行已输部分四态正确、光标态位于 pos；3) 检查第 2 行及之后全灰未输入态；4) 故意在行末/行首边界输错 2 字核对标色位置精确。
- **预期结果**：四态标色按全文 index 精确落到对应行对应列；跨行边界处标色无偏移。

#### TC-ROW-003 每行输入行只显示该行输入切片
- **优先级/类型**：P0 · 功能　**对应验收项**：A17
- **前置条件**：已输入跨 2 行以上字符（含错字）。
- **测试步骤**：1) 检查第 0 行下方输入行仅显示第 0 行范围（start..start+len）的输入字符；2) 检查第 1 行输入行仅显示属于第 1 行的输入；3) 检查输入行字符与原文行逐列对齐（等宽）；4) 错字在输入行原样显示（与原文行标红位置对应）。
- **预期结果**：输入行内容 = `userInput.slice(row.start, row.start+row.chars.length)` 的已输入部分；不串行、不越行显示；列对齐。

#### TC-ROW-004 未激活行输入行为空/虚线占位
- **优先级/类型**：P1 · UI　**对应验收项**：A17
- **前置条件**：输入至第 1 行（第 2 行及之后未到达）。
- **测试步骤**：1) 检查未到达行的输入行区域不显示任何输入字符；2) 核对其为空或虚线占位形态（对照 P1/P2）；3) 检查已输满的前序行输入行保留该行历史输入。
- **预期结果**：未激活行输入行空/虚线占位；前序行输入行保留；视觉与原型一致。

#### TC-ROW-005 idle 态：第 0 行 active + 起始光标 + 输入提示
- **优先级/类型**：P1 · UI　**对应验收项**：A17
- **前置条件**：新加载文章，未输入。
- **测试步骤**：1) 检查第 0 行为 active（蓝色边框高亮）；2) 检查第 0 行首字为橙色闪烁起始光标；3) 检查第 0 行输入行位置显示提示文案「点击此处开始输入，支持中文输入法」；4) 输入首字符后检查提示隐藏；5) 输入 2 字后连续 Backspace 退格回 pos=0（输入清空）：检查起始提示重显（Q3 裁决口径）。
- **预期结果**：idle 态三要素（active 行/起始光标/提示文案）齐备且符合 §6.7；首字符入库后提示隐藏；退格回 pos=0 且无输入时提示重显（Q3 已裁决并实现）。

#### TC-ROW-006 active 行高亮边框随 pos 迁移
- **优先级/类型**：P0 · UI　**对应验收项**：A17
- **前置条件**：输入中。
- **测试步骤**：1) 输入过程中观察 active 蓝框（2px `--accent` + 轻微发光）始终位于 pos 所在行；2) DevTools 核对非 active 行无蓝边框；3) 亮色主题下重复观察（对照 P5）。
- **预期结果**：任一时刻仅一个 active 行带蓝框；随 pos 实时迁移；暗/亮主题下均正确。

#### TC-ROW-007 active 行自动滚入视图（长文）
- **优先级/类型**：P0 · 功能　**对应验收项**：A17, B1
- **前置条件**：300 字长文（行数超出一屏）。
- **测试步骤**：1) 持续输入至第 4 行以后，不手动滚动；2) 观察 active 行变化时行列表是否自动滚动使 active 行可见；3) 检查滚动平滑、无跳动/回顶；4) 手动滚离后 active 行再次推进时仍被滚入。
- **预期结果**：active 行始终自动滚入视图（scrollIntoView nearest/smooth 或等价）；长文无需手动滚动即可持续练习。

#### TC-ROW-008 输满一行 active 自动推进下一行
- **优先级/类型**：P0 · 功能　**对应验收项**：A17
- **前置条件**：输入至第 0 行倒数第 2 字。
- **测试步骤**：1) 输入第 0 行末字：检查 active 仍在第 0 行或瞬时推进；2) 再输入 1 字（进入第 1 行）：检查 active 蓝框切到第 1 行、第 1 行输入行开始填充、第 0 行输入行冻结保留；3) 核对光标态位于第 1 行对应列。
- **预期结果**：行输满后 active 按 §6.8 派生规则推进下一行；前序行保留标色与输入；推进过程无闪烁错位。

#### TC-ROW-009 退格跨行回退
- **优先级/类型**：P0 · 功能　**对应验收项**：A17
- **前置条件**：输入刚进入第 1 行 1–2 字。
- **测试步骤**：1) 连续 Backspace 退过第 1 行首字、回到第 0 行末字范围；2) 检查 active 蓝框回退到第 0 行；3) 检查第 0 行输入行恢复显示（末字被删后缩短）、第 1 行输入行变空/占位；4) 继续退格核对标色同步回退。
- **预期结果**：退格使 pos 落入上一行时 activeRowIndex 自动回退；上一行重新 active 并恢复输入行显示；无残留状态。

#### TC-ROW-010 最后一行输满触发完成
- **优先级/类型**：P0 · 功能　**对应验收项**：A17, A6
- **前置条件**：输入至末行倒数第 1 字。
- **测试步骤**：1) 输入全文最后一字；2) 检查末行输满、active 行无下一行可推进；3) 检查 `done=true` 触发完成弹窗（内容核对见 TC-TYPE-011）。
- **预期结果**：末行输满即判定完成（pos === text.length）；弹窗正常弹出；行列表标色全部定格。

#### TC-ROW-011 输入中跨越 768px 断点的行重切分（自适应）
- **优先级/类型**：P2 · 边界　**对应验收项**：A17, A13, B5
- **前置条件**：桌面宽屏输入至第 2 行中部。
- **测试步骤**：1) 保持输入状态将窗口从 1200px 缩至 700px（跨断点，matchMedia 768px 信号 + ResizeObserver 触发，debounce 120ms 后重算）；2) 检查行列表按新容器宽度由镜像重测量重切分（行字符数由容器实际宽度决定，不再固定 14 字/行）、无横向溢出；3) 检查已输入字符标色与各行输入行在重切分后仍与原文逐列对应、pos/active 行正确、已输入内容不丢；4) 再放大回桌面宽度复查填充率恢复。
- **预期结果**：行字符数由容器实际宽度决定；除末行外行文本填充率 ≥90%；resize 后行重切分且已输入内容不丢；跨断点重切分后标色/输入行/active/进度全部自洽、无错位无丢字、无横向溢出。

#### TC-ROW-012 输入位置指示条随输入/退格/换篇实时更新
- **优先级/类型**：P1 · 功能　**对应验收项**：A17, A5
- **前置条件**：打字视图；Console 记录当前文章总字数 N。
- **测试步骤**：1) idle 态检查指示条显示起始行列（第 1 行·第 1 列口径）、「总 0/N 字」且进度 track 为 0；2) 输入跨行字符：检查「当前第 X 行·第 Y 列·总 Z/N 字」中 X 与 active 行、Y 与行内列偏移、Z 与 pos 同步正确，进度 track 填充 = Z/N；3) Backspace 若干字：检查指示条数值与 track 同步回退；4) 点击「换一篇」：检查指示条按新文章 N 重置归零。
- **预期结果**：指示条行/列/总字数与进度 track 随输入、退格、换篇实时准确更新，与 pos 及行切分口径一致；无停滞、无错位。

#### TC-ROW-013 宽屏自适应填充率（首行字符数显著大于 22）
- **优先级/类型**：P1 · 功能　**对应验收项**：A17
- **前置条件**：窗口宽度 1280px；任选一篇文章（290–300 字优先）；DevTools 可测量 `.row-source` 内容区宽度与各行文本宽度。
- **测试步骤**：1) 记录首行原文字符数 C1 与首行文本宽度/容器宽度 = 填充率 F1；2) 与旧固定方案 22 字/行对照：检查 C1 显著大于 22（自适应换行生效）；3) 抽样前 3 行（除末行外）填充率：检查均 ≥90%；4) Console 核对 `lineStarts`/`rows` 派生一致（行边界 = 镜像测量 top 变化点，全文全局索引）。
- **预期结果**：1280px 下首行字符数显著大于 22，行字符数由容器实际宽度决定；除末行外行文本填充率 ≥90%（宽屏约 40% 填充问题消除）；行派生与镜像测量口径一致。

#### TC-ROW-014 连续 resize 1280→768→375→1280 重切分不丢已输入
- **优先级/类型**：P1 · 边界　**对应验收项**：A17, A13, B5
- **前置条件**：1280px 输入跨 3 行（含 1 错字），记录 pos 与已输入字符序列；pos-indicator 可见。
- **测试步骤**：1) 连续 resize 1280→768→375→1280（每步停留 > debounce 120ms）；2) 每步后检查：行列表按当前容器宽度重切分、无横向溢出，已输入字符标色与各行输入行不丢、与原文逐列对应；3) 检查 active 行 = 包含 pos 的行、pos-indicator 行列/总字数与当前行切分口径一致；4) 最终回 1280px 与 resize 前状态对照：pos/userInput/charStates 完全一致（全局索引不变），继续输入正常。
- **预期结果**：连续 resize 全程行重切分且已输入内容不丢；active 行与 pos-indicator 始终正确；pos/userInput/charStates 按全文全局索引在重切分间自洽；无错位、无丢字、无 Console 报错。

#### TC-ROW-015 行切分守卫（镜像宽度异常/空文章不崩溃、rows 保持有效）
- **优先级/类型**：P2 · 边界　**对应验收项**：A14, B15
- **前置条件**：DevTools 可模拟镜像宽度异常（如临时将 `.row-source` 容器宽度置 0 后恢复）；空文章路径以 Console 数据契约核对（见 TC-TYPE-014）。
- **测试步骤**：1) 镜像宽度 <=0 期间触发重算（如容器宽度 0 时 resize）：检查守卫生效——保留上一次 rows，行列表不空闪/不塌缩、无 Console 报错；2) 恢复宽度并触发重算：检查 rows 正确重测量更新；3) 模拟 lineStarts 为空路径（首帧/测量未完成）：检查 rows 保持有效（保留上一次或安全初值）、无 NaN/undefined 行边界；4) 空文章数据路径：检查进度/指示条/统计显示安全值、无 NaN（与 Q5 口径一致）。
- **预期结果**：镜像宽度 <=0 或 lineStarts 为空时保留上一次 rows、不崩溃、rows 保持有效；宽度恢复后重测量正常；全守卫路径无 NaN（除零守卫与 B15 一致）。

### 3.4 模块：中文 IME（TC-IME）

#### TC-IME-001 中文组合输入整句 commit 逐字入库零错位
- **优先级/类型**：P0 · 功能　**对应验收项**：A7
- **前置条件**：切换微软拼音中文态；聚焦隐藏 input。
- **测试步骤**：1) 键入拼音组合（如 `nihao`）候选窗出现期间检查原文标色/pos 不变；2) 选词 commit 2 字：检查 2 字逐字入库、标色按对错正确、pos +2；3) 整句模式一次 commit 5 字（含 1 字与原文不同）：检查 5 字依次落位、错字标红且仅进输入行；4) 检查隐藏 input 值处理后被清空（无残留叠加）。
- **预期结果**：组合期不误触发入库；commit 后经 compositionend 逐字入库；零错位、无重复、input.value 清空。

#### TC-IME-002 组合期间拼音字母不误入英文
- **优先级/类型**：P0 · 功能　**对应验收项**：A7
- **前置条件**：中文态，新文章。
- **测试步骤**：1) 键入拼音 `zhong` 不 commit，观察输入行与标色；2) 检查原文行无 z/h/o/n/g 任何字母入库、pos 不变；3) Esc 取消组合后复查仍无字母残留。
- **预期结果**：组合期 keydown（keyCode 229 / isComposing）被跳过；拼音字母绝不出现在输入行或标色中。

#### TC-IME-003 组合期间退格删拼音不异常
- **优先级/类型**：P0 · 边界　**对应验收项**：A7, B8
- **前置条件**：中文态，已正确输入 3 字；再键入拼音 `wo` 处于组合中。
- **测试步骤**：1) 组合期按 Backspace 删除候选拼音 1 个字母：检查已入库的 3 字不被误删、pos 不变；2) 继续退格清空候选：检查组合态正常结束、isComposing 复位；3) 再按一次 Backspace：检查此时才正常删除已入库第 3 字；4) 全程无 Console 报错。
- **预期结果**：组合期退格仅作用于候选拼音，不触发正文退格；组合结束后退格恢复正常；无脏数据/不越界。

#### TC-IME-004 IME 候选中途取消（Esc）无脏数据
- **优先级/类型**：P1 · 边界　**对应验收项**：B8
- **前置条件**：中文态，组合中输入了 4 字拼音候选。
- **测试步骤**：1) 按 Esc 取消候选；2) 检查输入行/标色/pos 与取消前一致（无拼音残留入库）；3) 立即继续正常中文输入与英文输入各一次：检查均正常入库（isComposing 已复位）。
- **预期结果**：取消后无残留拼音、isComposing=false、input.value 清空；后续输入通道畅通。

#### TC-IME-005 中英混合：英文/数字非组合态直接逐字入库
- **优先级/类型**：P0 · 功能　**对应验收项**：B14, A7
- **前置条件**：文章含或手动构造对照（原文为中文字位）。
- **测试步骤**：1) 英文态直接键入 3 个字母/数字：检查逐字即时入库、对应原文位标红（错字）并显示在输入行；2) 切回中文态输入 2 字 commit：检查正常入库；3) 交替中英 3 轮检查无串态、无重复入库。
- **预期结果**：非组合输入走 @input 直接逐字入库；中文走 compositionend；中英切换零错位、零重复。

#### TC-IME-006 compositionend 与 input 事件不重复入库
- **优先级/类型**：P1 · 回归　**对应验收项**：A7
- **前置条件**：中文态；DevTools 准备观察 pos 变化。
- **测试步骤**：1) commit 一个 2 字词语：检查 pos 恰好 +2（非 +4 或 +3）；2) commit 一个 6 字整句：检查 pos +6；3) 英文态键入 1 字符：检查 pos +1（input 通道正常未被误跳过）。
- **预期结果**：commit 字符数与 pos 增量严格相等（isComposing 期间 @input 被跳过，防双通道重复）；英文通道不受影响。

#### TC-IME-007 isComposing 异常中断自愈（建议真机抽测）
- **优先级/类型**：P2 · 边界　**对应验收项**：A7, B8, B19
- **前置条件**：中文 IME；本路径（仅发 compositionstart、缺失 compositionend）自动化难稳定复现，**标注建议真机抽测**；正常组合流程回归不受影响。
- **测试步骤**：1) 真机：进入组合后通过快速切换输入法/切走窗口焦点等制造 compositionend 缺失的异常中断；2) 随后直接键入英文字符与 Backspace：观察输入通道是否恢复（以浏览器 `e.isComposing` 为权威自愈）；3) 重复 3 轮确认无「组合卡死、字符永不入库」；4) 回归正常组合 commit 流程确认零错位不变。
- **预期结果**：异常中断后 isComposing 依 `e.isComposing` 权威自愈，后续英文/退格正常入库、不永久卡死；正常组合流程行为不变；无 Console 报错。

### 3.5 模块：键盘全键测试（TC-KB）

#### TC-KB-001 104 键布局与五区分组完整
- **优先级/类型**：P0 · UI　**对应验收项**：A8
- **前置条件**：切到键盘测试 Tab。
- **测试步骤**：1) Console 核对 `TP_KEYBOARD_LAYOUT.length === 104` 且 code 唯一；2) 界面核对键帽网格渲染 104 键、宽键（Space/Shift/Enter/Backspace）跨列正确；3) 核对五区（功能键区/主键区/编辑区/方向键/数字小键盘）分组标注齐备（对照 P4）；4) 核对头部标题+环形进度+重置按钮布局。
- **预期结果**：104 键数据与渲染完备、分区清晰、键帽立体感符合 §8.7；无键缺失/重叠/溢出。

#### TC-KB-002 功能键区按键命中高亮
- **优先级/类型**：P0 · 功能　**对应验收项**：A8
- **前置条件**：键盘视图激活。
- **测试步骤**：1) 依次按 Esc、F1–F12（浏览器占用键以按下即命中为准；F5/F12 按 Q6 裁决放行会触发刷新/DevTools，该两键安排最后验或刷新前单独确认命中）；2) 检查对应键帽即时高亮（按下反馈）并转为已测绿色持久态；3) 核对环形进度与「已测试 N/104」同步 +对应数量。
- **预期结果**：功能键区逐键命中、高亮正确；进度计数准确。

#### TC-KB-003 主键区按键命中（含宽键）
- **优先级/类型**：P0 · 功能　**对应验收项**：A8
- **前置条件**：键盘视图激活。
- **测试步骤**：1) 按字母 A–Z 抽样 8 键、数字 0–9 抽样 4 键；2) 按宽键：Space、ShiftLeft、ShiftRight、Enter、Backspace、Tab、CapsLock；3) 检查各键帽命中高亮且位置与物理键对应（code 映射正确，非错位到邻键）。
- **预期结果**：主键区含宽键全部按 code 精确命中；左右 Shift 分别计数；无映射错位。

#### TC-KB-004 编辑区与方向键命中高亮
- **优先级/类型**：P1 · 功能　**对应验收项**：A8
- **前置条件**：键盘视图激活。
- **测试步骤**：1) 按 Insert/Delete/Home/End/PageUp/PageUp 对应六键与 PrintScreen（如系统可捕获）；2) 按方向键 ↑↓←→；3) 检查编辑区与方向键区对应键帽高亮、进度同步。
- **预期结果**：编辑区 6 键 + 方向 4 键全部命中高亮；计数准确。

#### TC-KB-005 数字小键盘命中高亮
- **优先级/类型**：P1 · 功能　**对应验收项**：A8
- **前置条件**：键盘视图激活；NumLock 开启与关闭各测一轮。
- **测试步骤**：1) NumLock 开：按小键盘 0–9、.、+、-、*、/、Enter；2) NumLock 关：按小键盘方向/编辑复用键位；3) 检查小键盘区键帽命中高亮与计数。
- **预期结果**：小键盘区按键按 code 命中（NumLock 两态不产生错误映射或崩溃）；高亮与计数正确。

#### TC-KB-006 已测键持久高亮
- **优先级/类型**：P0 · 功能　**对应验收项**：A8
- **前置条件**：已测试约 10 键。
- **测试步骤**：1) 松开所有键后检查已测键保持绿色高亮+发光（非仅按下瞬时态）；2) 等待 30 秒/点击页面其他区域后复查高亮不消失；3) 对照 P4 已测键视觉（绿+box-shadow 发光）。
- **预期结果**：testedKeys 持久高亮直至重置；视觉符合 §8.7 键帽已测态。

#### TC-KB-007 进度「已测试 N/104」与环形百分比准确
- **优先级/类型**：P0 · 功能　**对应验收项**：A8
- **前置条件**：键盘视图。
- **测试步骤**：1) 按 13 个不同键：检查文本「已测试 13/104」、环形中心百分比 ≈13%（13/104=12.5% 取整口径核对）；2) 重复按已测键 5 次：检查 N 不增长；3) 按至 42 键对照 P4 形态（40% + 42/104）。
- **预期结果**：N = testedKeys.size 精确；百分比与 N/104 一致；重复键不重复计数。

#### TC-KB-008 重置清空高亮与进度
- **优先级/类型**：P0 · 功能　**对应验收项**：A8
- **前置条件**：已测试 ≥20 键。
- **测试步骤**：1) 点击「重置」；2) 检查所有键帽恢复未测态、文本「已测试 0/104」、环形 0%；3) 重新按 3 键检查从 3 开始计数。
- **预期结果**：重置彻底清空 testedKeys 与进度；后续计数从零开始；无残留高亮。

#### TC-KB-009 打字视图按键不被键盘测试误捕获
- **优先级/类型**：P0 · 功能　**对应验收项**：B12, A9
- **前置条件**：当前为打字视图。
- **测试步骤**：1) 在打字视图输入 20 字练习；2) 切到键盘视图：检查「已测试 0/104」、无任何键被预先高亮；3) 回到打字视图继续输入再切回键盘复查仍为 0（除非期间在键盘视图按过键）。
- **预期结果**：KeyboardTest 的 document keydown 仅在组件存活期生效；打字视图的输入绝不计入键盘进度。

#### TC-KB-010 切走再切回监听正确重挂、无重复监听
- **优先级/类型**：P1 · 回归　**对应验收项**：A9, B12
- **前置条件**：键盘视图已测 5 键。
- **测试步骤**：1) 切到打字视图输入 5 字；2) 切回键盘视图按 1 键：检查该键命中且计数仅 +1（非 +2，验证无重复监听叠加）；3) 反复切换 Tab 5 轮后每按 1 键计数始终 +1；4) Console 无报错。
- **预期结果**：onMounted/onUnmounted 配对生效：切回可正常命中、单次按键单次计数、无监听泄漏叠加。

#### TC-KB-011 同键重复按/长按不重复计数
- **优先级/类型**：P1 · 边界　**对应验收项**：A8, B2
- **前置条件**：键盘视图，进度 0。
- **测试步骤**：1) 长按 KeyA 2 秒（触发 keydown 重复）；2) 检查「已测试 1/104」且 KeyA 高亮不闪烁异常；3) 快速连击 KeyS 10 次：检查计数为 2/104。
- **预期结果**：testedKeys 集合去重：重复/长按 keydown 不重复计数、不崩溃、高亮稳定。

#### TC-KB-012 布局外按键不计入；104 标准键全可捕获无保留标注
- **优先级/类型**：P2 · 边界　**对应验收项**：B13
- **前置条件**：键盘视图，进度 0。
- **测试步骤**：1) 按多媒体键（音量+/-、播放/暂停）与 Fn（如硬件产生事件）：检查进度仍 0/104、无异常高亮、无 Console 报错；2) 分区抽按 104 标准键（可多轮累计至全覆盖）：检查全部经 `e.code` 命中高亮，界面无任何「系统保留/不可捕获」标注（Q2 裁决：保留键不存在）；3) 对照 Console `TP_KEYBOARD_LAYOUT` 的 code 清单核对无遗漏键。
- **预期结果**：布局外 code 不命中、不计入、不崩溃；104 标准键全部可经 `e.code` 捕获高亮，无保留键标注（Q2 已闭环）。

#### TC-KB-013 键盘视图 preventDefault 策略（防焦点丢失/滚动与头部可达性）
- **优先级/类型**：P1 · 功能　**对应验收项**：A8, A14, B18
- **前置条件**：键盘视图激活；页面具备可滚动高度。
- **测试步骤**：1) 依次按 Tab、空格、方向键：检查焦点不离开键盘监听（后续按键仍命中）、页面不随空格/方向键滚动，且这些键本身正常计入已测；2) 按 F5/F12：检查放行（F5 触发刷新、F12 触发 DevTools），未被 preventDefault；3) 聚焦头部（.app-bar）按钮后按 Tab/空格：检查放行——Tab 可移动焦点、空格可激活按钮，头部控件键盘可达；4) 回打字视图按 Tab：检查隐藏 input 不丢焦（B18 打字视图口径）；5) 焦点回到键盘视图主体复测步骤 1 行为不变。
- **预期结果**：键盘视图对 Tab/空格/方向键 preventDefault（防焦点丢失/页面滚动）且键正常计入；F5/F12 放行；事件源自 .app-bar 头部控件时 Tab/空格放行保可达性；打字视图 Tab 不丢焦（Q6 已裁决并实现，B18 全口径覆盖）。

### 3.6 模块：主题切换与持久化（TC-THEME）

#### TC-THEME-001 首次访问缺省暗色
- **优先级/类型**：P0 · 功能　**对应验收项**：A10
- **前置条件**：清空 localStorage（或全新浏览器 profile）。
- **测试步骤**：1) 全新 profile 打开页面；2) 检查 `document.documentElement` 的 `data-theme="dark"`、背景 #1a1a1a、主题按钮为 🌙；3) 检查 Application 面板：localStorage 无 `tp_theme` 键（首屏不落盘）；4) 核对整体视觉对照 P1/P2 暗色。
- **预期结果**：无存储值时缺省暗色；data-theme 与视觉一致；首屏打开不写 localStorage（仅实际切换时落盘，见 TC-THEME-004）。

#### TC-THEME-002 一键暗↔亮切换全元素一致
- **优先级/类型**：P0 · UI　**对应验收项**：A10
- **前置条件**：暗色态，打字视图输入中（含四态标色+active 行）。
- **测试步骤**：1) 点击主题按钮切亮色：核对背景 #f5f5f7、卡片/row #ffffff、主文字 #1a1a1a、次文字 #6e6e73、按钮变 ☀️；2) 检查 row 容器、active 蓝框、四态色（绿/红/橙/灰）、进度条、统计药丸全部同步换色（对照 P5）；3) 切到键盘视图重复切换核对键帽/环形进度换色；4) 再切回暗色核对还原。
- **预期结果**：一键切换即时生效；全元素颜色一致、无遗漏硬编码色块；暗亮往返无残留。

#### TC-THEME-003 颜色全走 CSS 变量（data-theme 驱动）
- **优先级/类型**：P1 · 回归　**对应验收项**：A10, A16
- **前置条件**：DevTools Elements/Styles 面板。
- **测试步骤**：1) 审查 style.css：`:root` 亮色变量 + `[data-theme="dark"]` 覆盖；2) 抽查组件样式规则颜色均引用 `var(--bg/--card-bg/--text/...)`，无硬编码 hex（除变量定义处）；3) 切换主题时 Elements 面板确认仅 data-theme 属性变化、计算样式随变量切换。
- **预期结果**：颜色 100% 经 CSS 变量；切换仅靠 data-theme 属性驱动；符合 §8.3。

#### TC-THEME-004 localStorage['tp_theme'] 写入正确
- **优先级/类型**：P0 · 功能　**对应验收项**：A11
- **前置条件**：暗色态。
- **测试步骤**：1) 首屏打开（不做任何切换）检查 localStorage 无 `tp_theme`（仅用户实际切换才落盘）；2) 切亮色后检查 `tp_theme === "light"`；3) 切回暗色检查 `tp_theme === "dark"`；4) 检查键名严格为 `tp_theme`、值仅 dark/light 两取值。
- **预期结果**：首屏未切换不落盘；用户每次实际切换同步写库；键名/取值符合 §7.3 契约（裁决口径：仅切换时写）。

#### TC-THEME-005 刷新后主题保持
- **优先级/类型**：P0 · 功能　**对应验收项**：A11, B9
- **前置条件**：已切为亮色且写库。
- **测试步骤**：1) F5 刷新页面；2) 检查启动即为亮色（data-theme=light、视觉亮色）；3) 切暗色再刷新复查保持暗色；4) 刷新后打字/键盘两视图主题均正确。
- **预期结果**：主题偏好跨刷新保留；启动读取 localStorage 生效；无先闪缺省色再跳变（见 TC-THEME-008）。

#### TC-THEME-006 localStorage 禁用（隐私模式）静默降级
- **优先级/类型**：P1 · 边界　**对应验收项**：B6
- **前置条件**：浏览器隐私/无痕窗口且设置禁用站点数据（或 DevTools 模拟 storage 异常）。
- **测试步骤**：1) 打开页面检查正常渲染（缺省暗色）无崩溃；2) 连续切换主题 4 次：检查界面即时切换正常、无 Console error（try/catch 吞异常）；3) 刷新页面：检查回退缺省暗色且不报错。
- **预期结果**：读写失败静默降级；切换功能可用（仅不记忆）；主流程零阻塞。

#### TC-THEME-007 localStorage 写入超额静默降级
- **优先级/类型**：P2 · 边界　**对应验收项**：B7
- **前置条件**：Console 预置大体积数据占满配额（或 DevTools 模拟 QuotaExceededError）。
- **测试步骤**：1) 填满配额后切换主题；2) 检查切换即时生效、无未捕获异常、练习/键盘功能不受影响；3) 刷新后回退可读值或缺省暗色。
- **预期结果**：写库抛错被 try/catch 捕获；UI 与主流程不受影响；降级路径同 B6。

#### TC-THEME-008 切换与首载无闪屏
- **优先级/类型**：P2 · UI　**对应验收项**：A10
- **前置条件**：已存亮色偏好。
- **测试步骤**：1) 刷新页面肉眼+录屏观察首屏是否先暗后亮跳变；2) 切换主题观察是否整页同步换色（无局部延迟白块/黑块）；3) 慢速 CPU 节流 4x 下重复一次。
- **预期结果**：首载主题在首帧即正确（无明显 FOUC 跳变）；切换全程无闪屏/局部错色。

### 3.7 模块：视图切换（TC-TAB）

#### TC-TAB-001 打字↔键盘 Tab 切换与选中态样式
- **优先级/类型**：P0 · 功能　**对应验收项**：A9
- **前置条件**：默认打字视图。
- **测试步骤**：1) 点击「键盘测试」Tab：检查视图切换为键盘网格、pill 选中态为蓝底白字胶囊迁移；2) 点击「打字练习」切回：检查打字视图渲染；3) 往返 5 次检查切换稳定、未选中项为透明+主文字。
- **预期结果**：`<component :is>` 切换正常；分段 pill 选中态样式符合 §8.7；切换无白屏/错乱。

#### TC-TAB-002 Tab 切走再切回状态重置（V1 预期基线）
- **优先级/类型**：P1 · 功能　**对应验收项**：A9
- **前置条件**：打字视图输入 30 字；键盘视图另轮次已测 10 键。
- **测试步骤**：1) 打字输入 30 字后切键盘再切回打字：检查 pos=0、输入行清空、统计回初始值（状态重置）；2) 键盘视图测 10 键后切打字再切回键盘：检查「已测试 0/104」、无残留高亮；3) 切换链路检查无报错、无监听泄漏（结合 TC-TAB-004）。
- **预期结果**：按 Q1 裁决口径（`<component :is>` 无 keep-alive、unmount 重初始化）：切走再切回后打字进度与已测键**重置**，属 V1 预期行为而非缺陷；重置彻底、无残留状态、无报错。

#### TC-TAB-003 切换后无并排残留、仅一个视图存活
- **优先级/类型**：P1 · 回归　**对应验收项**：A9
- **前置条件**：DevTools Elements 面板。
- **测试步骤**：1) 打字视图下检查 DOM 中不存在 KeyboardTest 的键网格节点；2) 切键盘后检查 DOM 中不存在 TypingPractice 的 row 列表/隐藏 input 节点；3) 检查布局无并排残留（无隐藏占位撑高页面）；4) 往返切换 3 轮复查 DOM 节点数稳定不累积。
- **预期结果**：任一时刻仅一个视图组件存活于 DOM；无 display 切换残留与节点累积。

#### TC-TAB-004 反复切换无定时器/监听泄漏
- **优先级/类型**：P1 · 回归　**对应验收项**：B17, A9
- **前置条件**：打字视图已开始计时（输入过首字符）。
- **测试步骤**：1) 完成弹窗场景：输满全文后观察弹窗用时冻结（clearInterval 生效）；2) 打字计时中切走键盘再切回、重复 10 轮；3) DevTools Performance/内存快照或长时间观察：无间隔回调累积迹象、Console 无报错；4) 切走后原打字计时不再后台推进（切回后统计按重初始化基线表现）。
- **预期结果**：完成时与 onUnmounted 均 clearInterval；10 轮切换无定时器/监听泄漏、无性能劣化。

### 3.8 模块：边界与异常（TC-EDGE）

#### TC-EDGE-001 超长文章（300 字）多行切分+行列表滚动
- **优先级/类型**：P0 · 边界　**对应验收项**：B1, A17, A14
- **前置条件**：通过「换一篇」/刷新选取 290–300 字文章（Console 核对长度）。
- **测试步骤**：1) 核对行列表切分为多 row（行数由镜像测量自然换行派生，不再固定 24 字/行；除末行外填充率 ≥90%）且容器可滚动；2) 手动滚动行列表检查滚动手感与渲染完整；3) 输入至中段核对逐字标色与总进度条比例正确（pos/300）；4) 结合 TC-ROW-007 验证自动滚入。
- **预期结果**：300 字完整自适应切分、行列表可滚动、标色/进度精确、渲染无卡顿（keyed v-for diff 正常）；行字符数由容器实际宽度决定、除末行外填充率 ≥90%。

#### TC-EDGE-002 快速连续输入不丢字不错位
- **优先级/类型**：P0 · 边界　**对应验收项**：B2, A14
- **前置条件**：英文态（排除 IME 变量）与中文整句 commit 各一轮。
- **测试步骤**：1) 英文态以最快手速连击输入 40 字符（含正确与错误）；2) 核对输入行字符数 = 实际按键数、pos 同步、无丢字；3) 标色序列与按键序列逐位对照；4) 中文态快速连续 commit 3 组词语复查。
- **预期结果**：快速输入零丢字、零错位、不越界；Vue 响应式更新跟手、帧率稳定。

#### TC-EDGE-003 退格至起点不越界
- **优先级/类型**：P0 · 边界　**对应验收项**：B3, A14
- **前置条件**：输入 5 字；另轮次 idle 态直接测。
- **测试步骤**：1) idle 态（pos=0）连按 Backspace 5 次：检查无报错、pos 保持 0、界面不变；2) 输入 5 字后连按 Backspace 8 次：检查退到 0 后停止、不出现负数/异常标色；3) 检查统计/进度在 pos=0 时显示合法值。
- **预期结果**：`pos<=0` 守卫生效：退格至起点无效且不越界、不报错；后续输入可正常继续。

#### TC-EDGE-004 特殊字符/标点不破坏 DOM 与对比
- **优先级/类型**：P1 · 边界　**对应验收项**：B4, A14
- **前置条件**：选取含标点文章；Console 可构造对照字符串。
- **测试步骤**：1) 正常输入文章中的标点字位（，。等）核对标色与对比正确；2) Console 检查 `TP_ARTICLES` 文本若含 `<>&"'` 类字符：核对页面以纯文本显示、未注入 DOM 节点（Vue 插值转义）；3) 英文态键入 `<`、`&` 到错字位：检查输入行原样显示、DOM 结构不被破坏。
- **预期结果**：特殊字符经文本插值默认转义；显示/对比/DOM 三者均正常；无 XSS/结构破坏。

#### TC-EDGE-005 窗口缩放与 768px 窄屏响应式
- **优先级/类型**：P1 · 边界　**对应验收项**：A13, B5, A14
- **前置条件**：桌面宽度打开，两视图各准备数据（打字输入中/键盘已测若干键）。
- **测试步骤**：1) 窗口从 1440px 逐步缩至 768px 以下（含 375px 抽测）：检查键盘 Grid 不溢出不撑破（可缩放/换行策略核对）、统计条换行显示、行列表不横向溢出；2) 窄屏下打字/键盘/弹窗/主题切换全流程操作一遍；3) 放大回桌面复查布局还原。
- **预期结果**：768px 断点生效：窄屏键盘 Grid 不溢出、统计条换行、布局不错乱；全功能窄屏可用；行列表行字符数由容器实际宽度决定、resize 后行重切分且已输入内容不丢（自适应换行口径）。

#### TC-EDGE-006 完成后继续输入/退格被忽略
- **优先级/类型**：P0 · 边界　**对应验收项**：B10, A6, A14
- **前置条件**：完成弹窗已显示。
- **测试步骤**：1) 弹窗期间键入 10 个字符与 3 次 Backspace；2) 检查弹窗不消失、行列表标色定格不变、无字符入库；3) 点击「再试一次」后检查输入通道恢复、可正常输入。
- **预期结果**：`done` 守卫生效：完成后所有输入（含退格）被忽略；仅「再试/换一篇」可恢复输入。

#### TC-EDGE-007 粘贴文本被禁用
- **优先级/类型**：P1 · 边界　**对应验收项**：B11, A14
- **前置条件**：剪贴板已复制一段文章原文；打字视图聚焦。
- **测试步骤**：1) Ctrl+V 粘贴：检查无字符入库、pos 不变；2) 右键菜单粘贴（如隐藏 input 可触发菜单）复查；3) 粘贴后继续手动输入检查通道正常。
- **预期结果**：`@paste.prevent` 生效：粘贴不绕过逐字对比；正常输入不受影响。

### 3.9 模块：视觉规范（TC-VIS）

#### TC-VIS-001 等宽字体逐字列对齐
- **优先级/类型**：P1 · UI　**对应验收项**：A16
- **前置条件**：打字视图输入跨 2 行；DevTools 检查 computed font-family。
- **测试步骤**：1) 核对字体栈为 `"SF Mono","Consolas","PingFang SC","Microsoft YaHei",monospace`；2) 检查每行输入行字符与原文行字符逐列垂直对齐（无半字偏移）；3) 中英文混输场景复查对齐不漂移。
- **预期结果**：等宽保证原文行/输入行列对齐；字体栈符合 §8.4；未引入 Inter/Roboto 等外部字体。

#### TC-VIS-002 配色符合第 8 章配色表（暗/亮抽样）
- **优先级/类型**：P1 · UI　**对应验收项**：A16, A10
- **前置条件**：暗/亮两态各截图 + DevTools 取色。
- **测试步骤**：1) 暗色抽样：主背景 #1a1a1a、row 卡片 #2d2d2d、主文字 #f5f5f7、次文字 #98989d、accent #0a84ff；2) 亮色抽样：#f5f5f7/#ffffff/#1a1a1a/#6e6e73；3) 四态色暗亮同值抽样：#30d158/#ff453a/#ff9f0a；4) 核对弹窗阴影/圆角规格（16px 圆角、暗 0 8px 24px rgba(0,0,0,.35)）。
- **预期结果**：抽样色值与 §8.1/8.2/8.7 完全一致；状态色暗亮同值。

#### TC-VIS-003 8px 间距体系与组件形态规格抽检
- **优先级/类型**：P2 · UI　**对应验收项**：A16
- **前置条件**：DevTools 测量模式。
- **测试步骤**：1) 抽检卡片内边距 24px、元素间距 16px、row 内边距 12px 16px、行间距 12–16px；2) 抽检 pill Tab 高约 40px/圆角 999px、row 圆角 12px、统计药丸圆角 999px、进度条高 4–6px；3) 核对字号层级（原文逐字 18–20px、弹窗主数值 40–56px）。
- **预期结果**：间距/圆角/字号符合 §8.5–8.7 规格区间；无显著偏离。

#### TC-VIS-004 键帽立体感/环形进度/弹窗形态对照原型抽检
- **优先级/类型**：P2 · UI　**对应验收项**：A16
- **前置条件**：键盘视图已测若干键；完成弹窗可触发；对照 P3/P4。
- **测试步骤**：1) 核对键帽圆角 8px、上亮下暗内阴影立体感、已测键绿+发光；2) 核对环形进度为描边环+中心百分比+「已测试 N/104」；3) 核对完成弹窗：绿色成功徽章、标题「练习完成」、速度主数值大号强调色、主/次按钮层级（实底胶囊/描边）。
- **预期结果**：组件级形态与 P3/P4 原型及 §8.7 描述一致；视觉细节无缺失。

---

## 4. 附录：设计澄清裁决与验证闭环记录（Q1–Q6 闭环、Q7 已裁决待验证）

> V1.0 提出的 6 个设计澄清点已全部经 Leader 裁决；实现按裁决完成，并通过浏览器 E2E + 静态回归验证。V1.2 按 PM 裁决7 新增 Q7（自适应换行）：裁决与算法口径已冻结，验证状态**待验证**（浏览器 E2E 回归由任务 #13 在代码完成后执行并闭环）。相关用例预期结果已同步对齐裁决口径（见「闭环用例」列）。

| # | 类型 | 问题摘要 | 裁决结果（Leader） | 验证状态 | 闭环用例 |
| --- | --- | --- | --- | --- | --- |
| Q1 | 矛盾 | Tab 切换「状态保持」要求与 §5.2 unmount 重初始化语义冲突 | 采用设计基线：`<component :is>` 无 keep-alive，切走切回打字进度/已测键重置 = V1 预期（非缺陷） | 已验证（E2E） | TC-TAB-002（预期改为「切换后状态重置」） |
| Q2 | 矛盾 | §7.2/B13「系统保留/不可捕获键」标注与 104 标准矛盾 | 不存在保留键：104 标准键全部可经 `e.code` 捕获高亮、无任何保留标注 | 已验证（E2E） | TC-KB-012（删除保留标注预期，改为全可捕获核对） |
| Q3 | 遗漏 | 退格回 pos=0 时起始提示是否重显未定义 | 已实现：退格回到 pos=0 且无输入时重显起始提示 | 已验证（E2E） | TC-ROW-005（新增重显核对步骤） |
| Q4 | 遗漏 | 中间宽度（768px–桌面）溢出策略未说明 | 已实现：768px 断点 + 中间宽度 word-break 优雅换行、不横向溢出 | 已验证（375/768/1280 实测无溢出） | TC-ROW-011, TC-EDGE-005 |
| Q5 | 遗漏 | 空文章/除零无防御，progress 可能 NaN | 已实现：进度/速度/准确率均有除零守卫，任何情况不出现 NaN | 已验证（E2E + 静态回归） | TC-TYPE-006/008/014（预期明确「安全值，绝无 NaN」） |
| Q6 | 遗漏 | 键盘视图浏览器键 preventDefault 策略未说明 | 已实现：Tab/空格/方向键 preventDefault 防焦点丢失/滚动、放行 F5/F12；事件源自 .app-bar 头部控件时放行 Tab/空格保键盘可达 | 已验证（E2E） | TC-KB-013（新增）, TC-KB-002（F5/F12 放行测试顺序提示） |
| Q7 | 矛盾 | 宽屏下每行原文只占行宽约 40%（根因：固定字符数切行，桌面 22 字/行、≤768px 14 字/行） | 裁决改自适应换行（PM 裁决7）：隐藏镜像元素 `.row-mirror` 承载全文，逐字符 Range 取 `getClientRects()[0].top`，top 变化处为新行起点得 `lineStarts[]`，`rows` 由其派生（全文全局索引）；镜像与 `.row-source` 共享排版类 `.line-text` 且 `white-space: pre-wrap`（防空格折叠测量偏差，历史坑）；镜像宽度 = `.row-source` 内容区宽度；重算触发 = ResizeObserver + window resize + matchMedia 768px 变化（仅信号）+ document.fonts.ready，debounce 120ms；守卫 = 镜像宽度 <=0 或 lineStarts 为空保留上一次 rows；状态安全 = pos/userInput/charStates 全局索引、activeRowIndex 为包含 pos 的行查找、重切分不丢已输入 | **待验证**（待任务 #13 E2E 闭环） | TC-ROW-013/014/015（新增）, TC-ROW-001/011、TC-EDGE-001/005（预期修订为自适应口径） |

**闭环结论**：Q1–Q6 全部「已裁决 + 已验证」；Q7「已裁决、待验证」（由任务 #13 浏览器 E2E 回归闭环后更新验证状态）。QA 判定基线：自适应换行相关以裁决7 算法口径为唯一口径，其余以本文档已闭环用例预期为口径。

*文档结束 · test-cases.md V1.2（自适应换行对齐版） · 测试工程师 Taylor*
